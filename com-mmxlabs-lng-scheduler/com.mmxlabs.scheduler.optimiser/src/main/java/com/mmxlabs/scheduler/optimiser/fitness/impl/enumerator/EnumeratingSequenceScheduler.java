package com.mmxlabs.scheduler.optimiser.fitness.impl.enumerator;

/**
 * A sequence scheduler which enumerates possible combinations of arrival times
 * explicitly, rather than using the GA byte array decoding method. This should be subclassable
 * into a random sequence scheduler as well, with reduced decoding overhead
 * @author hinton
 *
 */
public class EnumeratingSequenceScheduler {

}
